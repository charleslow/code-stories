{
  "name": "code-stories-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/viewer",
    "build": "npm run build --workspace=packages/viewer",
    "preview": "npm run preview --workspace=packages/viewer"
  }
}
